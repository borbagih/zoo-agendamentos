// calendar.js
// Simulação de calendário simples

document.addEventListener("DOMContentLoaded", () => {
    const container = document.getElementById("calendarContainer");
    if (container) {
        container.innerHTML = "<p>Calendário ainda em construção... (simulação)</p>";
    }
});
